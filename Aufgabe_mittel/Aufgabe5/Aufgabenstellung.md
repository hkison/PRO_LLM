### **Gültige Klammerfolge**  

**Aufgabe:**  

Prüfe, ob der gegebene String aus den Klammern `()`, `{}`, `[]` eine gültige Reihenfolge aufweist.

**Input Format:**  

- Eine einzelne Zeile mit dem String, der nur die Zeichen `()`, `{}`, `[]` enthält.

**Output Format:**  

- `true` oder `false`.

**Beispiel:**  

**Input:**  

({[]})  

**Output:**  

true  
